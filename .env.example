# Backend
PORT=5000
NODE_ENV=production
CORS_ORIGIN=*
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=200
MONGODB_URI=
MONGODB_DB=mini_soc

# Frontend (used by Vite during build)
# Leave empty for same-origin '/api' in single-service deploys
VITE_API_BASE_URL=